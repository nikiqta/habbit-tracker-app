import AppText from '@components/ui/AppText';
import Screen from '@components/ui/Screen';

export const HabitListScreen: React.FC = () => {
  return (
    <Screen>
      <AppText variant="h1">Habit List Screen</AppText>
    </Screen>
  );
};
