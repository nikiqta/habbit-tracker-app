import AppText from '@components/ui/AppText';
import Screen from '@components/ui/Screen';

export const HabitDetailsScreen: React.FC = () => {
  return (
    <Screen>
      <AppText variant="h1">Habit Details Screen</AppText>
    </Screen>
  );
};
